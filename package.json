{
  "name": "leet-code",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "bench": "vitest bench --run --reporter verbose --outputFile.json reports/benchmarks.json"
  },
  "author": "Bohdan Hishka",
  "license": "MIT",
  "dependencies": {
    "vitest": "^1.4.0"
  }
}

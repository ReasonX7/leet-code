{
  "name": "leet-code",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cli": "node scripts/cli/index.mjs",
    "test": "vitest run",
    "bench": "vitest bench --run --reporter verbose --outputFile.json reports/benchmarks.json"
  },
  "author": "Bohdan Hishka",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "vitest": "^1.4.0"
  }
}
